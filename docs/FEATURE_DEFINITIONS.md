# Feature Definitions

Feature names should be descriptive enough to communicate meaning without opening implementation code.

Preferred conventions:

- use unit or scale in the name when relevant
- distinguish cycle-level vs event-level vs parent-context features
- avoid renaming semantics without updating this document and changelog
