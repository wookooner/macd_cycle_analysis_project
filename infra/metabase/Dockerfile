# Metabase now runs from the official image via compose.yaml.
# This file is intentionally kept as a marker because earlier DuckDB-driver
# experiments used a custom build. It is no longer part of the active setup.
